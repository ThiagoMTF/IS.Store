<nav class="navbar navbar-expand-lg navbar-dark bg-info">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">IS Store</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <div class="ml-0">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Home")">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Produtos")">Produtos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Sobre", "Home")">Sobre</a>
                    </li>
                </ul>
            </div>
            <div class="mr-0">
                <ul class="navbar-nav">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <div class="dropdown">
                            <a class="btn btn-secondary dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                @User.Identity.Name
                            </a>

                            <ul class="dropdown-menu">
                                <li><a class="dropdown-item" href="#">Trocar Senha</a></li>
                                <li><a class="dropdown-item" href="#">Perfil</a></li>
                                <div class="dropdown-divider" href="#"></div>
                                <li><a class="dropdown-item" href="@Url.Action("Logout", "Conta")">Logout</a></li>
                            </ul>
                        </div>
                    }
                    else
                    {

                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Login", "Conta")">Efetuar Login</a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </div>
</nav>